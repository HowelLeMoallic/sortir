<nav>

    
    {% if app.user %}
        <ul>
            <li>
                <a href="{{ path('accueil') }}">Accueil</a>
            </li>
            <li>
                <a href="{{ path('profil_connecte', {'id' : app.user.id}) }}">Mon profil</a>
            </li>
            <li>

                <a href="{{ path('app_logout') }}">Déconnexion</a>


            </li>
        </ul>
    {% endif %}

</nav>