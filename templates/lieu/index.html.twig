{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Création de lieu{% endblock %}

{% block body %}

    {{ form_start(formLieu) }}
        {{ form_widget(formLieu) }}
        <section>
            <button type="submit">
                Envoyer
            </button>


            <a href="{{ path('creation_sortie') }}"><button type="button">Annuler</button></a>

        </section>
    {{ form_end(formLieu) }}


{% endblock %}
